
server:
  port: 8089

management:
  endpoints:
    web:
      exposure:
        include: "*"


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/mydatabase
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true